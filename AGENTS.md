# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

When explaining something, include easy-to-grasp examples where useful.

This app preserves sensitive location observations. Never introduce fabricated observations, silent coordinate edits, analytics, or plaintext uploads. Do not describe records as legally certified, perfectly accurate, unhackable, or independently timestamped unless that exact claim has been established. Keep native background behavior and evidence verification separate from the UI. Run typechecking, evidence tests, and native bundle checks after relevant changes. Native bundling is not physical-device validation.
